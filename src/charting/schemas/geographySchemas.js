import { chartSchema, role } from "./schemaTypes.js";
const roles = (required = false) => [role("geography", "Geographic identifier", ["geographic", "category", "text"], 1), role("value", "Value", ["number"], 1), role("time", "Time", ["temporal"], required ? 1 : 0)];
const base = { group: "geography", form: { sections: ["data", "appearance", "interactions", "advanced"] }, dataFamily: "geography", renderer: "geography", capabilities: { timeSync: true, collection: false, zoom: true }, conversions: [] };
export const geographySchemas = [chartSchema({ ...base, typeId: "choroplethMap", label: "Choropleth", description: "Show a value by geographic region.", roles: roles(), semantics: { purpose: "geography", mark: "region" } }), chartSchema({ ...base, typeId: "chronoChoroplethMap", label: "Chronological choropleth", description: "Show how geographic values change over time.", roles: roles(true), semantics: { purpose: "geography", mark: "temporal-region" } }), chartSchema({ ...base, typeId: "mapScatter", label: "Map scatter", description: "Show measured points or values at geographic locations.", roles: roles(), semantics: { purpose: "geography", mark: "map-point" } })];
