import { chartSchema, role } from "./schemaTypes.js";
const roles = () => [role("row", "Row category", ["category", "text"], 1), role("column", "Column category", ["category", "text", "temporal"], 1), role("value", "Value", ["number"], 1), role("time", "Time", ["temporal"], 0)];
const base = { group: "readiness", form: { sections: ["data", "appearance", "labels", "interactions", "advanced"] }, dataFamily: "matrix", renderer: "matrix", capabilities: { timeSync: true, collection: false, zoom: false }, conversions: [] };
export const readinessSchemas = [chartSchema({ ...base, typeId: "heatmap", label: "Heatmap", description: "Show the intensity of values across two categories.", roles: roles(), semantics: { purpose: "readiness", mark: "heatmap-cell" } }), chartSchema({ ...base, typeId: "readinessMatrix", label: "Readiness matrix", description: "Show readiness states across entities and criteria.", roles: roles(), semantics: { purpose: "readiness", mark: "matrix-cell" } })];
