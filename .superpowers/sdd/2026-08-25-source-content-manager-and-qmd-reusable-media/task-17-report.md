# Task 17 verification report

Date: 2026-08-26
Base HEAD: `739188d93aed1cc2a118251b37af49a92daa1751`

## Result

Task 17 is not complete. It made no production, test, generated-output, or fixture changes. It records the prescribed one-pass evidence exactly and leaves integrated promotion open.

## Preflight and Step 7S

- Worktree was clean before execution.
- All seven retained Playwright files existed.
- The existing exact parser scoped to `## Final Step 7S controlling 36-row disposition` returned 36 rows, 36 unique expected IDs, and zero non-Passing Engine/UI/Fidelity cells: **36/36 Passing**.

## Deterministic selection

The specified Task 17 `node --test` selection ran once: **674 tests, 649 passing, 25 failing, 0 skipped/todo** in 25.74 seconds.

- 20 failures stopped during sandboxed Vite/esbuild configuration loading with `Cannot read directory ../../../../../..` and an unresolved worktree `vite.config.js`.
- `dashboardBundleV3` failed raster-signature validation for a declared authored payload.
- `datasetProfilesV3` has three assertion failures: two expected rejections did not occur, and one expected public runtime shape omits `runtimeContentHealth`.
- PS-04 could not start the copied Windows flash-package launcher on a port in its configured range.

No tests or production code were changed to mask or fix those results.

## Browser selection and inspected checkpoint

The one required seven-file Chromium invocation used no extra literal separator. It discovered 15 tests, reported **Journey C passing**, and then returned without the normal aggregate or results for the remaining cases. Therefore the only retained browser acceptance is C; A, B, D, E, F, G, H, I, J, and K are **not evaluated / not promoted**.

The retained Journey C trace verifies local blob-only media rendering, media revision `3`, QMD safe-DOM host semantics, selection/inspector routing, caption/frame, and Build at 1440×900 with bounded media geometry. This is behavioral/DOM/geometry evidence, not a screenshot-only claim.

## Remaining concerns

1. The integrated deterministic gate is red (649/674), so its affected amendment rows cannot receive Task 17 fidelity promotion.
2. The browser runner did not return an aggregate after C. A future authorized run must diagnose the runner/fixture termination before accepting the independent A–K evidence.
3. PS-04 remains unresolved and was not cleared; build/generated-client refresh belongs to an explicit pre-merge/release authorization.
4. No full build, full unit suite, full Playwright suite, `dist` edit, or fixture change was performed.
