import assert from "node:assert/strict"; import test from "node:test"; import { readFile } from "node:fs/promises"; test("failed async tree rename retains the inline draft for retry",async()=>{const source=await readFile(new URL("../src/components/build/BuildStructureRail.jsx",import.meta.url),"utf8");assert.match(source,/const saved = await onRename\?\.\(selectionFor\(node\), value\);[\s\S]*if \(!saved\) return;[\s\S]*cancelRename\(node\);/);});
