# Spike Manifest

## Idea

Calibrate bounded GeoJSON resource and nesting limits for the approved Source Content Manager/QMD reusable-media amendment before implementation planning, using the current legitimate corpus, deterministic independent fixture ladders, current production validation/package/map owners, and actual Chromium measurements.

## Requirements

- The calibration is planning evidence only and must not modify production code, production tests, dependencies, manifests, lockfiles, generated catalogues, or shared production CSS.
- Warning and hard limits come from measured performance/resource behavior with margin above all legitimate project GeoJSON files; encoded bytes alone never decide acceptance.
- One future validation authority must apply the limits with a bounded iterative checker; join compatibility/coverage remains a replacement outcome.
- The written amendment remains unimplemented and no fidelity row is promoted by this spike.

## Spikes

| # | Name | Type | Validates | Verdict | Tags |
|---|---|---|---|---|---|
| 001 | geojson-limit-calibration | standard | Given the four legitimate GeoJSON files and bounded independent ladders, when current validation, packaging, persistence, and ECharts paths run in normal and constrained Chromium profiles, then measured knees support exact warning/hard limits without excluding legitimate inputs or creating a material UX tradeoff. Master corrections add a demonstrated 32 MB warning/36 MB hard byte-property margin, distributed part/ring evidence, an exact whole-document node budget, and per-feature key semantics. | VALIDATED — second corrected result submitted | geojson, chromium, limits, performance, security |
