---
context: default
phase: dashboard-icon-language-design
task: 3
total_tasks: 5
status: paused
last_updated: 2026-08-04T08:16:44.057Z
---

# Dashboard icon-language design checkpoint

<current_state>
Paused at the safest boundary between design synthesis and artifact creation. The code-backed control audit is complete, the application strategy is decided, and the comprehensive visual icon atlas has not yet been written. There is no partial atlas file to recover.

Active worktree: `C:\Users\hekma\Documents\SimEx Dashboard\.worktrees\simex-dashboard-v2\dashboard-refinement-round-2`

Branch and HEAD: `codex/dashboard-refinement-round-2` at `c8563d1`

No production source files were changed and no test suite was run during this design iteration.
</current_state>

<completed_work>

Completed Tasks:

- Task 1: Explored the current repository, interaction specifications, current branch, and project policy. `packaged-dashboard-bundle.json` was absent.
- Task 2: Completed three independent read-only audits:
  - Shell/page/edit-mode/chart-panel/common-dialog actions.
  - Wizard/editor/source-viewer/color and authoring actions, including all 26 chart-type cards.
  - Playback/fullscreen/multi-selection/collection controls and state variants.
- Task 3: Synthesized the application policy and icon-language rules.
- Iteratively designed and visually approved:
  - Clock plus dominant play triangle for the synchronized-playback entry control.
  - One dashboard-wide customizable base accent with automatically derived light- and dark-surface variants.
  - Approved reset arrowhead in `accent-color-editor-v11.html`: triangular, 7 degrees clockwise from v10, about 5.6 × 6.2 pixels when the SVG renders at 21 pixels, with clean arc overlap.

The approved visual grammar is:

- Rounded 24 × 24 SVG geometry, normally 1.8–1.9 stroke width.
- Neutral/navy structure on light surfaces and white structure on dark surfaces.
- One accent-filled or accent-stroked focal datum per glyph.
- Base accent defaults to `#19D3C5`; readable context variants are derived automatically.
- Danger and selection semantics override the customizable accent when needed.
- Icon-only controls always retain a tooltip, accessible name, and visible focus state.
</completed_work>

<remaining_work>

- Task 4: Create `icon-language-atlas-v1.html` in the existing visual-companion session, covering:
  - Three application strategies: aggressive icon-only, recommended layered iconography, and conservative icon-supporting-text.
  - Shell and edit-mode controls.
  - Playback states: toggle, previous step, play, pause, next step, speed, and clearly marked future-only rewind/fast-forward/jump controls.
  - Fullscreen and multi-fullscreen states, including unselected/selected fullscreen buttons, 1–4 selected-panel indicators, enter/cancel, close, reorder, and all layout diagrams.
  - Chart-panel actions: source information, source CSV, fullscreen, edit, section insertion, removal, zoom reset, export/snapshot, and future reference-line action.
  - Wizard/editor actions and supporting step/tab icons.
  - Collection modes: fixed grid, scrollable grid, periodic carousel, continuous scroll, priority, pager, pause/resume, loop, and re-ranking.
  - Supporting pictograms for all 26 registered chart types; labels and descriptions remain visible.
  - Default, hover/focus, active, selected, disabled, busy, and destructive variants.
- Task 4 verification: visually inspect the catalog at normal scale and key glyphs enlarged. Do not run application test suites.
- Task 5: Show the atlas to the user, collect visual feedback, iterate with new versioned files, then record the approved design contract and transition to an implementation plan.
</remaining_work>

<decisions_made>

- Use a layered iconography approach. Universal and reversible actions may be icon-only; primary or ambiguous operations use icon plus short wording; confirmations and analytical choices keep explicit text.
- Do not convert page names, chart-type names, editor tab names, source propagation actions, save/create commitments, import/export, or confirmation choices to unlabeled icons.
- Distinguish step-back/step-forward from fast rewind/fast-forward. The current reducer does not implement true scanning controls; show those only as future glyphs.
- Keep numeric values visible for timestamps, playback speed, collection page status, selection counts, and analytical ranking methods.
- Keep the approved icon accent globally configurable, but use danger red for destructive focal marks and semantic green for selected multi-fullscreen panels.
- Do not modify production code until the atlas has been visually reviewed.
</decisions_made>

<blockers>

- No product blocker.
- `rg.exe` failed with access denied. Use `Get-ChildItem` plus `Select-String` for searches in this environment.
- The approved local worktree is outside the default writable sandbox root. Use `apply_patch` and scoped escalation where necessary. Never move work into OneDrive.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — branch, bundle-baseline, and agile verification policy.
2. This handoff and `.planning/HANDOFF.json` — exact pause state.
3. `.superpowers/brainstorm/1309-1785817774/content/accent-color-editor-v11.html` — approved accent and reset-icon baseline.
4. `docs/superpowers/specs/2026-07-29-chart-interaction-editor-redesign.md` — panel/editor interaction commitments.
5. `docs/superpowers/specs/2026-07-30-fullscreen-icon-controls-design.md` — fullscreen layout glyph requirements.
6. `docs/superpowers/specs/2026-07-30-multi-fullscreen-selection-design.md` — selection states and limits.
7. `docs/app-manual.md` — current moderator-visible behavior and chart catalogue.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat every text button as an icon-only candidate. Preserve words where the glyph would hide scope, consequence, or analytical meaning.
- Do not show future transport icons as currently implemented behavior.
- Do not overwrite existing companion files. Create a new semantic versioned filename for every visual iteration.
- Do not run broad tests during this visual-design loop; project policy defers them until pre-merge approval.

## Infrastructure State

- Visual companion project session: `.superpowers/brainstorm/1309-1785817774`
- Last URL: `http://localhost:56139/?key=d804ba36709c67b22e39b8cc530917ab3fda765472ce919cfd360468bfb2a0d0`
- Before pause: `server-info` existed and `server-stopped` did not. A system restart will terminate it; restart with the same project directory so the session/port can reconnect.
- No dashboard development server or test watcher was started for this task.
- No external async jobs are running.

## Advisory constraints discovered through failure

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| `rg` unavailable | `rg.exe` returned access denied during the audit. | advisory | Use PowerShell `Get-ChildItem` and `Select-String`; do not waste time repeatedly retrying `rg`. |
| First parallel audit connection dropped | All three audit streams disconnected once, then completed after explicit retry. | advisory | If a resumed subagent stream drops, retry the same bounded task once before redoing the audit locally. |

## Pre-Execution Critique Required

- Design artifacts: approved `accent-color-editor-v11.html` and the synthesized inventory in this handoff.
- Critique focus: whether every icon-only proposal is understandable without persistent text; whether future-only glyphs are visibly separated; whether selected, disabled, danger, and busy states remain distinguishable on both light and dark surfaces.
- Gate: do not begin production implementation until the visual atlas has been shown and revised from user feedback.

<context>
The intended catalog is a polished, long-form visual atlas rather than a loose list. It should let the user inspect the approved language across realistic button sizes and state combinations. Recommended top-level strategy is “layered iconography,” with aggressive and conservative alternatives shown for comparison. Include current exact control wording beneath each proposed glyph so the list remains auditable.
</context>

<next_action>
Start with: check whether the existing companion server survived; after a restart, relaunch it using the same project directory. Then create `icon-language-atlas-v1.html`, render the complete audited catalog, and visually inspect it before giving the user the keyed URL.
</next_action>
