# Source Content Manager and QMD Reusable Media — Proposed Security and Deviation Record

**Date:** 2026-08-25
**Status:** Proposed and unimplemented. User-approved architectural decisions await V3 Design master review and final user approval of the written amendment.
**Base status preserved:** the implemented Step 7S 36-row result was accepted by the V3 Design master at b366ba17fe856aede46ba8301b8a530520e4d2cd, with documentation closure at db63d8e772ce96b17de19b7a89f256a72926d08d. It remains Passing, independently review-clean, branch-retained, and unmerged. No decision below is implementation evidence.

## Proposed security and portability decisions

| ID | Proposed decision | Security/portability reason | Required verification | Status |
|---|---|---|---|---|
| SCM-SP01 | Use the layered dashboard-contained `contentLibrary` with the exact canonical keys `contentLibrary.mediaItems` and `contentLibrary.sourceEntries`. Logical media, CSV source-management metadata, content-addressed bytes, dataSources, and datasetProfiles retain separate authorities. | One payload store or alternate schema vocabulary would blur mutation, validation, retention, and portability boundaries. | Exact-key schema tests accept only `contentLibrary.mediaItems` and `contentLibrary.sourceEntries`, reject duplicated bytes/profiles and alternate/unknown keys, and preserve all layers through a V5 round trip. | Proposed / unimplemented / not verified |
| SCM-SP02 | Classify generated/intermediate sources only from trusted explicit provenance; never infer generated ownership from filenames or paths. | Filename heuristics can hide user-controlled data or expose dashboard internals as editable. | Adversarial names/paths remain builder-visible legacy-import unless trusted provenance says generated; dashboard-owned records remain hidden. | Proposed / unimplemented / not verified |
| SCM-SP03 | A committed media record is a durable asset reference even when unused. | A reusable library item must not disappear because it lacks a current placement. | Reference-graph and startup-cleanup tests preserve unused committed records and reclaim only deliberately deleted or abandoned staged content. | Proposed / unimplemented / not verified |
| SCM-SP04 | Physical image bytes remain content-addressed and deduplicated; logical records may remain distinct. CSV matches warn but never auto-dedupe. | Physical dedupe controls quota without silently collapsing author intent or source identity. | Same image hash adds zero duplicate bytes while two mediaIds remain valid; same CSV produces a warning and separate sourceId. | Proposed / unimplemented / not verified |
| SCM-SP05 | Delete is blocked by visible direct use, draft use, and active/restorable undo retention and never cascades. A disabled referenced Delete shows inline explanation/navigation and opens no dialog; only eligible Delete opens confirmation. | Cascading deletion or deletion behind restorable history could silently destroy meaning or create a dangling mediaId/sourceId. | Dependency graph distinguishes visible/draft/undo blockers; undo-only state offers explicit discard of relevant history; blocked Delete cannot open a modal; eligible confirmation and commit leave no dependent mutation or dangling ID. | Proposed / unimplemented / not verified |
| SCM-SP06 | Chrono groups, Scenes, and presentation compositions are downstream impact contexts, not direct source dependencies. Present messages and object-URL leases are transient. Managed CSV dependency is only a chart/panel whose primary sourceId is that CSV, including map charts; map.geoSource GeoJSON is outside this amendment. | Dependency counts must reflect durable ownership instead of double-counting indirect/runtime references or silently expanding management to GeoJSON. | Direct counts include specified QMD/Image visible uses, drafts, undo retention, and primary managed-CSV uses only; impact contexts appear only for temporal warnings; GeoJSON descriptors remain unmanaged. | Proposed / unimplemented / not verified |
| SCM-SP07 | Media replacement preserves mediaId, increments revision, and atomically publishes the new current asset without rewriting placement accessibility/layout metadata. Manager create/detail can set/edit the default description; it prefills only new QMD/Image placements. | Stable logical identity enables intentional global replacement without conflating library defaults with contextual authoring. | Failure leaves old revision/bytes/settings intact; success updates all uses together; default edits affect new-placement prefill but never rewrite existing alt; active leases remain safe until released/refreshed. | Proposed / unimplemented / not verified |
| SCM-SP08 | CSV replacement preserves sourceId only after parse, safety, size, profile, and direct-chart validation. Structural incompatibility is a hard block. | Retaining identity for unusable data would corrupt live charts. | Missing encoding columns and parse/safety failures make no change and offer Import as new source plus remapping. | Proposed / unimplemented / not verified |
| SCM-SP09 | Temporal-field, range, frame, and availability changes warn but do not block structurally valid CSV replacement. Confirmed replacement marks affected temporal configurations needs-review/degraded where appropriate. | Temporal changes can be intentional and should not be misclassified as structural corruption. | Warning names affected Chrono/Scene/presentation contexts; confirm commits atomically; cancel is no-op. | Proposed / unimplemented / not verified |
| SCM-SP10 | QMD creates an image only from a validated simex-media:mediaId destination backed by stored or packaged media. | QMD must remain portable and must not gain ambient network/local-resource authority. | HTTP/HTTPS/data/blob/file/malformed/external-only forms stay text and make zero requests; valid local form resolves through a leased object URL. | Proposed / unimplemented / not verified |
| SCM-SP11 | QMD media attributes are an exact allowlist with tokenized styles. | Arbitrary CSS, markup, events, or positioning would violate the permissive-inert source boundary. | Unknown/duplicate/out-of-range attributes remain inert; runtime DOM has no authored style/class/event authority. | Proposed / unimplemented / not verified |
| SCM-SP12 | Existing HTTPS-linked media remains External / Network required and standalone-Image-only. **Create local copy** creates a new stored mediaId after local upload or browser-permitted direct HTTPS fetch passes full validation; it leaves the external record and uses unchanged and selects the new QMD-eligible item. | Linked media is intentionally nonportable and must not undermine QMD offline or request-authority guarantees. | Direct fetch never uses a proxy, privilege escalation, silent fetch, or CORS bypass; unavailable fetch requires local upload; validation failure creates nothing; success returns a distinct selected local ID while external Image uses remain exact. | Proposed / unimplemented / not verified |
| SCM-SP13 | Dashboard schema and package bundle become V5 with exact `contentLibrary.mediaItems` and `contentLibrary.sourceEntries` keys, V4 import remains compatible, and chart configuration remains V3. | Logical content-library identity and retained unused media change dashboard/package envelopes but not per-chart shape. | Migration runs before V5 validation; V4 fixtures produce canonical exact-key V5; exact version assertions remain synchronized. | Proposed / unimplemented / not verified |
| SCM-SP14 | Bundle/package V5 includes every retained stored/packaged `contentLibrary.mediaItems` record and validates IDs, revisions, hashes, MIME, dimensions, animation state, QMD references, `contentLibrary.sourceEntries`, dataSources, and datasetProfiles atomically. | A reusable library is portable only if unused retained items and all reference edges survive. | Missing/corrupt/invalid payload aborts before replacement; successful offline exact-key round trip retains used and unused media. | Proposed / unimplemented / not verified |
| SCM-SP15 | Manager uploads commit only on Add to dashboard; authoring-flow uploads remain application-session drafts and commit with the completed panel/chart. | An abandoned authoring flow must not create unexplained durable library/source entries. | Cancel cleans only staged draft content; successful panel/chart commit publishes contentLibrary/source/bytes atomically; unsaved state is absent after reload. | Proposed / unimplemented / not verified |
| SCM-SP16 | Missing/corrupt identity and dependencies persist until explicit repair; failed CSV refresh/replacement retains the last committed source/profile. | Failure must not erase provenance, dependency navigation, or the last known-good state. | Health transitions are typed; repair is explicit; siblings and saved state remain usable. | Proposed / unimplemented / not verified |
| SCM-SP17 | Labels, names, captions, descriptions, provenance, and imported metadata render as text. | Management surfaces introduce new authored/imported strings that must not become markup. | Mounted DOM contains text only, no authored elements/attributes/resources, across list, detail, dialogs, captions, and errors. | Proposed / unimplemented / not verified |
| SCM-SP18 | Startup cleanup respects committed media records, sources, drafts, restorable undo blockers, and transactions. | Cleanup must distinguish durable library retention from abandoned staging, temporary undo retention, and transactional recovery. | Reference-graph fixtures cover every owner and exact cleanup boundary, including undo-only retention/discard and interrupted replacement/import, with no dangling mediaId/sourceId. | Proposed / unimplemented / not verified |

## Explicit deviation record

| ID | Existing accepted Step 7S decision | User-approved amendment | Consequence | Approval state |
|---|---|---|---|---|
| SCM-D01 | SP-06 and the QMD feature table make all embedded-image syntax inert. | One validated local form, ![alt](simex-media:mediaId), plus an allowlisted attribute subset creates a stored/packaged QMD image. All other destinations remain inert. | portable-qmd-v1 gains a bounded local-media extension without network, active markup, or arbitrary CSS authority. | Proposed written deviation; master/user review pending |
| SCM-D02 | SP-21 fixes dashboard schema V4 and bundle V4. | Dashboard schema and bundle become V5, V4 import remains compatible, chart configuration remains V3. | Requires canonical V4→V5 migration and synchronized package/storage/version evidence. | User-approved V4→V5 deviation; written master/user review pending |
| SCM-D03 | Static Image source identity directly owns origin assetId/path/url plus placement metadata. | Static Image placements reference stable mediaId in `contentLibrary.mediaItems`; logical media owns current asset/revision/default metadata, while placement owns alt/decorative/crop/rotation/fit and placement/surface-local transient zoom remains outside the media item. | Separates global reusable identity from contextual rendering/accessibility without changing Step 7S zoom persistence; the manager default description prefills only new placements. | Proposed written deviation; master/user review pending |
| SCM-D04 | SP-17 cleanup treats saved source/manifest reachability as durable ownership and reclaims unused durable assets. | A committed `contentLibrary.mediaItems` record is itself a durable reference even with zero placements; active/restorable undo retention temporarily blocks deletion until relevant history is explicitly discarded. | Unused committed library items survive cleanup/export/import until deliberate deletion, and deletion never leaves undo with a dangling mediaId/sourceId. | Proposed written deviation; master/user review pending |
| SCM-D05 | The Image design says asset replacement resets crop/rotation/fit by default and retains alt for review. | Replace library file everywhere preserves mediaId and all placement alt/decorative/crop/rotation/fit plus placement/surface-local zoom state. | Global replacement updates content revision without mutating contextual authoring; Change image here remains placement-scoped. | Proposed written deviation; master/user review pending |
| SCM-D06 | Step 7S has no dashboard-wide source manager or reusable-media catalogue. | Build gains a non-modal Source Content Manager and focused media pickers over `contentLibrary.mediaItems` and CSV-only `contentLibrary.sourceEntries`, while preserving the six-stage chart and four-stage static workflows. | Adds management UI without adding workflow stages, exposing generated/intermediate sources, or taking ownership of GeoJSON descriptors; builder-facing GeoJSON management may be added later. | Proposed addition; master/user review pending |
| SCM-D07 | Existing CSV validation is load/import oriented; no identity-preserving manager replacement policy is accepted. | Structural incompatibility hard-blocks; temporal/range/frame/availability differences warn and may mark downstream temporal contexts needs-review/degraded after confirmation. | Separates chart structural validity from intentional temporal change. | Proposed addition; master/user review pending |
| SCM-D08 | Existing bundles include referenced local image assets. | V5 packages include all retained managed stored/packaged media, including unused library records. | Portability scope expands from placement reachability to deliberate library retention. | Proposed written deviation; master/user review pending |
| SCM-D09 | Existing authoring uploads are panel-scoped and cleanup removes unused staged assets. | Manager Add to dashboard can deliberately commit an unused media item; authoring uploads remain draft-scoped until panel/chart commit. | Commit intent, not mere byte intake, distinguishes reusable durable content from abandoned staging and publishes only the exact contentLibrary records appropriate to the committed item. | Proposed addition; master/user review pending |
| SCM-D10 | Step 7S distinguishes linked Image portability but has no QMD media eligibility rule. | External HTTPS media remains standalone-Image-only; **Create local copy** creates a new stored mediaId through validated local upload or browser-permitted direct HTTPS fetch and leaves the external record and uses unchanged. | Preserves QMD offline/no-request guarantees while retaining linked Image support; no proxy, privilege escalation, silent fetch, or CORS bypass is introduced. | Proposed addition; master/user review pending |

## Preserved boundaries

The amendment does not change these accepted constraints:

- QMD is native and portable; Google Docs integration is deferred.
- Arbitrary authored text remains accepted and inert; no sanitizer is introduced.
- SVG, APNG, animated WebP, active markup, mismatched MIME, unsafe local paths, and arbitrary CSS remain rejected or inert.
- Unsaved drafts remain application-session-only.
- Static content remains outside Chrono Groups and Scenes.
- Free text remains excluded from Present/Audience; standalone Image remains the only static Present/Audience type.
- The six-stage Add chart and four-stage Add static content workflows remain exact.

## Approval gate

No proposed decision or deviation is Implemented or Passing. Master review must resolve this record and the companion specification/fidelity matrix, after which the user must approve the written amendment before implementation planning.
