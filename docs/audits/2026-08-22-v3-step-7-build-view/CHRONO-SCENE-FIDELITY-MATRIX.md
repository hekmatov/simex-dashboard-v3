# Sketches 005, 006, and 012 fidelity matrix

Date: 2026-08-24

Scope: Step 7 Build temporal authoring

Authority: accepted Sketches 005, 006, and 012 plus the user-approved Chrono reconciliation amendments dated 2026-08-22 and the Scene staging amendment dated 2026-08-24

## Gate

A row passes only when live production wiring has evidence for all three layers: semantic correctness, composition correctness, and real-use correctness. A pure engine, static markup, or uninspected screenshot cannot pass a row alone. If any row returns to Missing or Partial, the affected sketch must not be reported as implemented and Step 7 returns to not ready for master review.

## Binding matrix

| ID | Accepted invariant | Production owner | Deterministic check | Browser task | Viewports/states | Evidence | Status | Deviation record |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 005-01 | Chrono Studio uses the Staged Proof Studio: persistent identity/status, Name and period → Choose charts → Set defaults → Review, independently scrolling body, fixed actions, visible validation, and non-mutating close/restoration. | `App.jsx`; `DashboardRenderer.jsx`; `ChronoStudio.jsx`; `ChronoGroupContent.jsx`; `ChronoGroupEditor.jsx`; `chronoGroupDraft.js`; `BuildWorkspace.jsx`; `modes.css` | `chronoGroupEditor.test.js`; `buildWorkspaceV3.test.js`; `chronoSceneComposition.test.js`; exact `005-chrono-group-authoring` production-bundle journey | Use the global `Chrono Studio` command with Dashboard Map closed, open existing Group content, choose `Edit`, traverse all four stages, inspect the fixed footer, close, and retain the Build target. | 1200×900, 1280×720, and 768×1024; clean, dirty, validation error | The corrected named journey clicks the live Chrono Studio route and content-page Edit action; it contains no retired `Chrono Groups` locator. Clean-archive execution passes, and in-app inspection confirms Studio → content → populated editor. | Passing | User-approved amendment moves name into stage 1 and uses Chrono Group terminology. |
| 005-02 | Choose charts has Selected, Needs attention, and Available regions. A complete record moves without duplication and retains page/section, other-group membership, full range, in-period counts, expandable per-variable ticks on one period scale, and text status. Defaults expose the four matching policies, positive cadence, member fallback, and Review facts/repair routes. | `chronoGroupDraft.js`; `AvailabilityLedger.jsx`; `ChronoGroupEditor.jsx` | `chronoGroupEditor.test.js`; `chronoSceneFidelity.test.js`; `chronoSceneComposition.test.js` | Expand selected evidence, inspect aligned ticks and memberships, move to Review, and follow the owning-stage repair controls. | 1200×900 and 768×1024; expanded/collapsed, ready/needs-attention | Accepted scene 03 and the live production ledger were visually compared in-app. The production journey asserts all three regions, full range and plotted-variable summaries, expandable aligned evidence, Review facts/repair routes, compact-height clearance, and tablet geometry. | Passing | None. |
| 005-03 | Save is atomic; failure retains a retryable draft; discard restores baseline; duplicate produces new group/child-Scene identities without committing before Save; dirty exit resolution preserves focus/scroll/target and ordinary editor actions do not include Stay. | `chronoGroupDraft.js`; `chronoContentState.js`; `BuildWorkspace.jsx`; draft coordinator/restoration modules | `chronoGroupEditor.test.js`; `buildDraftCoordinator.test.js`; `buildWorkspaceV3.test.js`; production component integration in `chronoSceneComposition.test.js` | Exercise edit/read return in the live studio; semantic fixtures falsify failed-save, retry, suspension, duplicate ownership, and restoration. | Saving, error, suspended/conflict; 1200×900 | Focused transaction/restoration tests passed; live editor showed Save/Discard ownership and no ordinary Stay action. | Passing | Stay is retained only as a conflict-resolution choice, per 2026-08-22 approval. |
| 006-01 | Stage 1 is **Scene details** and owns name, Page → Parent Chrono Group → contained editable period → Time mode, Calendar interval or Frame source, matching default, and seconds/frame. Save readiness and atomic actions remain in a full-width transaction footer available in all stages. | `SceneEditor.jsx`; `sceneDraft.js`; `sceneSchema.js`; `modes.css` | `chronoSceneFidelity.test.js`; `chronoSceneComposition.test.js`; `sceneSchema.test.js` | Create Scene, complete Scene details, change dependent scope, narrow dates, switch Calendar/source and back, then continue without losing compatible state. | 1200×900 and 768×1024; Stage 1 and both time modes | Focused semantic/composition checks pass for the live three-stage model, details ownership, validation routing, and transaction footer. The named production-bundle journey and in-app inspection confirmed Stage 1 is the initial active stage and the form uses the available editor width. | Passing | User-approved 2026-08-24 amendment replaces the persistent side panel with Stage 1 and preserves dependency/containment rules. |
| 006-02 | Stage 2 reuses the full availability-ledger grammar: selected/attention/available regions, page/section identity, aligned chart/variable evidence, one Frame source, All vs Selected frames, and a separate scrollable observation checklist. It receives the full available editor width. | `SceneEditor.jsx`; `sceneDraft.js`; `temporalAuthoringData.js`; `BuildWorkspace.jsx`; `modes.css` | `temporalAuthoringData.test.js`; `chronoSceneFidelity.test.js`; `chronoSceneComposition.test.js` | Enter Stage 2, inspect variable ticks, assign a source, choose explicit observations, and confirm no sidebar reserves width. | Desktop/tablet; source/calendar; modal open | The production-bundle journey found no `.scene-draft-panel` and measured Stage 2 at more than 95% of its workspace width while retaining live variable evidence and availability timelines. | Passing | Renumbered from Stage 1 by the 2026-08-24 amendment; ledger behavior is unchanged. |
| 006-03 | Scene Studio uses the browser width with 16 px gutters. Stage 3 places actual-chart Scene View and Present canvases in separate full-width rows capped by the canonical canvas-width token, with independent membership/order, visible insertion targets, title drag/drop, Alt movement, chart-corner Present actions, chart-local floating Unit Orbit controls that remain simultaneously visible and clear of both the selected chart and Scene transaction footer, and count-valid 1–4 layouts. The saved Scene must reproduce the authored Scene projection in View and seed the authored Present subset/order/layout without exposing authoring chrome. | `BalancedTwinCanvas.jsx`; shared `UnitOrbit.jsx`; `SceneViewCompositionGrid.jsx`; `DisplayedChartGrid.jsx`; `scenePresentLayout.js`; `scenePresentTransition.js`; `DashboardRenderer.jsx`; `sceneDraft.js`; `SceneEditor.jsx`; `modes.css` | `unitOrbit.test.js`; Scene mapping/transition tests; exact `006-scene-authoring amendment` browser journey with viewport/canonical-width/orbit geometry and live Save | Enter Stage 3, confirm the editor reaches both viewport gutters and Scene View reaches the canonical canvas maximum, open Unit Orbit from a chart title and confirm the chart and transaction actions remain reachable, author distinct Scene/Present arrangements, save with the orbit open, activate in View, enter Present, round-trip modes, then reselect the Scene. | 1600×900 canonical-width proof; 1280×720 in-app inspection; 768×1024 stacked; selected target with orbit open; View; Present | Engine: Unit Orbit candidates now clip before a horizontally overlapping protected footer. UI: the Scene footer is part of the live protected-region set. Fidelity: the focused geometry test passes and the clean exact 006 journey saves with the orbit open, then completes View and Present reproduction. In-app inspection shows the selected map and orbit together without overlap. | Passing | The approved full-width amendment supersedes the earlier equal-width desktop side-by-side arrangement. The chart-local Unit Orbit correction removes the detached settings section. Scene selection in View does not mutate View Focus/Comparison state; first entry to Present seeds its independent presentation runtime. |
| 012-01 | Browsing is separate from mutation: Chrono Studio owns Create Chrono Group; Scene Studio and Chrono Group content own Create Scene; cards open read-first content; Edit opens the populated editor; page/parent/child names derive from one canonical temporal truth. | `ChronoStudio.jsx`; `SceneLibrary.jsx`; `ChronoGroupContent.jsx`; `SceneContent.jsx`; `chronoContentState.js`; `BuildWorkspace.jsx` | `chronoContentState.test.js`; `sceneStudio.test.js`; `chronoSceneComposition.test.js`; `v3-chrono-scene-fidelity.spec.js` | Studio → content → Edit → content → Studio for both object types; Chrono Group content → Create Scene; close and resume the unfinished Scene. | 1200×900 and 768×1024; ready, needs-attention, and suspended records | The clean named journey clicks Chrono Group content → Create Scene, verifies Scene Studio and the populated parent Group, enters Arrange, closes, then finds `Resume Scene draft` in the visible `Paused Build work` navigation and restores both canvases. | Passing | Explicit 2026-08-22 replacement of Sketch 012’s single Time content entry with separate studios; relationship visibility remains binding. |
| 012-02 | Ready, Needs attention, issue reasons/counts, true-empty, No Scenes yet, and filtered no-results are distinct without colour-only meaning. Edit, Duplicate, Remove, Repair, and one-draft conflict remain explicit and mutation waits for confirmation/commit; running playback state remains independent. | Content selectors/components; `chronoContentState.js`; `BuildWorkspace.jsx`; playback snapshot engines | `chronoContentState.test.js`; `chronoSceneComposition.test.js`; `buildDraftCoordinator.test.js`; immutable playback tests | Open ready/attention content, inspect reasons and lifecycle actions, create a Scene, return to its read-first page, and retain the existing browsing context. | Ready/attention/empty/no-results/conflict | Component integration asserted named reasons and lifecycle controls; controlled browser created and reopened real content; semantic tests prove canonical selection and conflict ownership. | Passing | None. |
| 012-03 | Search, Status, and Page filters show truthful counts and preserve independent Chrono/Scene queries, page scope, selection, scroll, and focus through authoring return. Desktop/tablet retain internal scrolling, essential reachability, and no document-level horizontal overflow. | `chronoContentState.js`; Studio/content components; `BuildWorkspace.jsx`; `modes.css` | `chronoSceneFidelity.test.js`; `chronoContentState.test.js`; `chronoSceneComposition.test.js` | Filter/search a Scene, open it, return, confirm the query, switch studios without query leakage, and repeat at tablet width. | 1200×900 and 768×1024; populated/empty/filtered | Browser journey asserted Search/Status/Page, truthful counts, query restoration, and tablet geometry; independent-studio context reducer test passed. | Passing | Phone Build remains intentionally unsupported. |

## Three-layer result

- **Semantic correctness:** focused state/schema/transaction tests passed for scope recomputation, containment, mode memory, independent canvas order, validation, save failure/retry, suspension/restoration, browse context, and canonical ownership.
- **Composition correctness:** production-integrated SSR tests passed for the complete 005 ledger/Review, 006 three-stage details/evidence/full-width twin canvases/Unit Orbit, and 012 filters/read-first/lifecycle composition.
- **Real-use correctness:** the three exact named Playwright journeys passed; desktop/tablet geometry was asserted, and the accepted winner sketches and live application were inspected side by side in the in-app browser.

## Completion gate

- Every in-scope row maps to live production code and evidence: **yes**.
- All three named browser journeys exist and pass independently: **yes**.
- Actual-application representative tasks were exercised and visually inspected: **yes**.
- No binding row is Missing or Partial: **yes**.
- Engine implemented, UI implemented, and fidelity verified are reported separately above: **yes**.
- Master acceptance remains outside this matrix and must be decided by V3 Design master review.

## 2026-08-24 committed-tree correction

The master-review failure at `7ee8f84d9d4f5d792f563e4c0e7bda6260498e80` invalidated earlier Passing evidence that depended on stranded dirty-overlay wiring or the retired 005 command. Production corrections are committed in `5e9952b` and `fd52c72`. A clean archive now resolves all production imports, the focused semantic/geometry checks pass, and the exact 005/006/012 journeys use the accepted live routes. These rows remain Passing only on that renewed committed evidence.
