# V3 Sketch 001–020 fidelity matrix

Date: 2026-08-23

Status: final Step 7 submission ledger; master acceptance not claimed

Authority: the approved exported Sketch 001–020 README/HTML pairs, accepted V3 specifications, and explicit later user-approved amendments. A row cannot pass on an isolated engine, static labels, or an uninspected screenshot alone.

| Sketch | Accepted invariant | Production owner | Deterministic check | Browser task | Viewports/states | Evidence | Status | Deviation record |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 001 — Audience output | Passive 1920×1080 output, count-valid 1–4 chart layouts, title placement, lifecycle states, and Scene date-position consumption/save. | Step 8 Present/Audience shell; saved Scene schema | Step 8 Audience contract tests | Present preview → move/save date → inspect Audience states | 1920×1080 logical canvas | Scene schema persists `datePosition`; Audience consumption absent. | Intentionally deferred | Step 8 owns runtime consumption; Step 7 only preserves the Scene field. |
| 002 — contextual panel editing | Live independent layout and selected-chart drafts coexist; each owns Save/Discard, validation, failure, suspension, and restoration; footprint stays chart-owned. | `App.jsx`; `BuildWorkspace.jsx`; live draft coordinator; chart editor | Live component integration plus draft reducer tests | Dirty layout → dirty chart → park auxiliary → resolve each independently → restore target/focus/scroll | 768×1024, 1024×768, 1200×900, 1440×900; dirty/error/suspended | `68b9c4d` wires separate production layout/chart slots and explicit layout Save/Discard; `fd21b06` completes auxiliary suspension/restoration. Focused semantic/composition tests pass 24/24; both representative browser journeys pass 2/2. The inspected 1365×1270 discard journey retained the dirty chart editor and restored its target to y=426–844. | Passing | Exact cross-mode rectangle equality is retired; canonical renderer/layout and restoration remain binding. |
| 003 — visual language | All Step 7 surfaces and interaction states use the approved style/profile tokens, typography, state grammar, tooltips, SVG/canvas paint, and non-colour status. | Shared tokens/styles; portal tooltip layer; chart renderers | Expanded retired-style scan across CSS, inline/SVG/canvas configuration, portals, pseudo-elements, hover/focus/disabled states | Traverse all Step 7 overlays; inspect representative hover/focus/error/disabled states in each style/profile | Desktop/tablet plus supported phone View; light/dark and all approved styles | `5c52f9e` adds a live full-document audit for computed paint, active borders/outlines, typography, generated content, body-level portals, SVG attributes/paint, and representative hover/focus/disabled/error states; canvas paint remains covered by the production-imported ECharts projection integration. The scan found and repaired retired loading shimmer, Structure/empty-recovery controls, Collection headers/cards, footer text, Scenario Passport actions, and obsolete Build appearance controls. Focused semantic/composition checks pass 90/90 and the affected browser suite passes 24/24 across all Pages, all three native styles, all 15 Light/Dark profiles, Build studios, wizard, Passport, Chrono, source viewer, and 390×844 View. In-app inspection confirmed the portaled tooltip uses the active profile, remains inside the viewport, and the loading state uses profile-derived paint. | Passing | No new aesthetic direction. Dashboard Look retains its approved transient-compression exemption; exact Build/drawer clearance and centered gutters while Look is open are not required. |
| 004 — chart creation | Exact six stages plus a persistent, independently reachable canonical-render proof and destination/placement proof deck throughout authoring; atomic creation and repair links remain stage-owned. | `ChartWizardV3.jsx`; chart draft/proof/review engines | Wizard component integration and proof-revision tests | Start draft → change destination/type/data/config → inspect both proofs from every stage → create/retry | 1200×900, 768×1024; valid/invalid/stale/error | `5e3e834` adds the persistent dual-proof deck with separate status/revision and projected order. Component proof tests pass 2/2; the all-six-stage proof journey and existing suspend/resume/create-once journey pass 2/2. In-app inspection at 1365×1270 measured an 818px stage body beside a 360px proof deck, independent revisions, and no root horizontal overflow. | Passing | No seventh Proof stage. `5285c82` removes the stale test assumption that the dialog name changes with its highlighted stage; the approved single title remains stable. |
| 005 — Chrono Group authoring | Four-stage Staged Proof Studio with persistent identity, complete three-region Availability Ledger, aligned evidence, copy semantics, atomic save/retry, and restoration. | Chrono editor/draft/ledger/content state | Existing Sketch 005 fidelity suite | Exact `005-chrono-group-authoring` journey | Desktop/tablet; dirty/error/suspended | Dedicated matrix and browser comparison pass. | Passing | Time Group renamed Chrono Group; name moved to stage 1; ordinary Stay removed. |
| 006 — Scene authoring | Persistent Scene Draft, real Stage 1 ledger, dependent scope/time controls, and interactive balanced Scene/Present canvases with insertion, Unit Orbit, and valid layouts. | Scene editor/draft/schema; twin canvas | Existing Sketch 006 semantic/composition suite | Exact `006-scene-authoring` journey | Desktop side-by-side; tablet stack; source/calendar | Dedicated matrix and browser comparison pass. | Passing | Name moved first; balanced twin canvas selected. |
| 007 — View Chrono | Real per-chart/frame availability, positive numeric cadence, time-proportional seek rail, group/Scene playback, provenance, matching, reveal, and draggable/resizable date overlay. | `ChronoController.jsx`; `PlaybackProvider.jsx`; `playbackAvailability.js`; `ChronoDateOverlay.jsx`; live canonical chart panels | Playback engine plus live component tests and exact View E2E | Select source/scope → inspect availability → set cadence → seek by time → move/resize overlay → move controller → play/pause | 1200×900, 768×1024, 390×844 View; missing/mixed/static/focus/comparison | `9b4dd79` replaces the hard-coded legend with real per-chart frame evidence and stable C-series IDs, retains discovered temporal roles in the default Page group, adds time-proportional rail ticks and a positive numeric cadence, outlines charts only while evidence is shown, and supplies the movable/resizable date overlay. Focused semantic/composition checks pass 59/59; the exact desktop/tablet/phone journey passes 1/1 with no document overflow. In-app inspection showed Concurrent, Missing, and Static records with observed-date counts and the same live controller/date. | Passing | None. |
| 008 — Present controller | Complete Live Sidecar lifecycle and passive Audience authority. | Step 8 Present workspace | Step 8 controller/Audience integration | Present session lifecycle → Audience | Desktop controller + Audience canvas | Foundations only; designated Step 8 implementation not run. | Intentionally deferred | Step 8. |
| 009 — shared shell | Layered Command Crown preserves location/mode continuity and the supported phone boundary. | Shared shell/crown/mode state | Existing shell tests; accepted-reference comparison | Switch pages/modes and retain active session/drafts | 390×844, 768×1024, 1024×768, 1200×900, 1440×900 | No structural mismatch found; visual comparison evidence is weaker than claimed. | Partial | Retired exact View/Build rectangles no longer apply; shared canonical renderer contract does. |
| 010 — Dashboard Look | Contextual drawer exposes accepted look/profile/chart-colour/appearance ownership, provenance, saved truth, Signature shortcuts, and scoped actions without geometry mutation. | Dashboard Look drawer/draft/theme state | Drawer ownership and persistence tests | Open → preview style/profile → use Signature → scoped applies → close/failure | Desktop/tablet; persistence success/failure; light/dark/system | Later close-and-apply works; other accepted controls were removed and tests encode absence. | Partial | Close/outside/Escape now applies selected style/profile asynchronously and never blocks closing; failure flashes a bottom-right warning. |
| 011 — Structure authoring | Approved Page/Section rename, reorder, move, remove, merge, consequence preview, and recovery commands are exposed in their containment. | `BuildPageNavigation.jsx`; `DashboardCanvas.jsx`; `SectionStructureCommandDialog.jsx`; `buildLayoutDraft.js`; live renderer draft transaction | `structureCommandModel.test.js`; `anchoredStructureCommands.test.js`; adjacent layout/structure regressions | Create/reorder/move/merge/remove Page/Section; review temporal consequences; preview across Pages; discard/restore | 1280×720 desktop; clean/dirty; bounded Orbit/dialog; destination Page | `8e1b65b` wires the active Page rail and exact Page Orbit into the Crown, makes Section titles direct rename triggers, exposes move/merge/remove on canonical Section headers, names affected charts/Chrono Groups/Scenes, and routes every command through the independent layout draft. Focused semantic/composition tests pass 11/11; the exact live E2E passes 1/1. In-app inspection exercised cross-Page movement and Discard restoration; the Page Orbit measured 300×236 and the Section dialog 420×556 inside 1280×720 with no root overflow. | Passing | The older generic Structure tree remains a coordinated navigation/secondary command surface; accepted local commands are no longer delegated to it. |
| 012 — temporal content | Page-grouped parent Chrono Group → child Scene relationships, read-first content, separate Studio creation ownership, filtering/context restoration, and explicit lifecycle actions. | Chrono/Scene Studios and content pages | Existing Sketch 012 fidelity suite | Exact `012-temporal-content` journey | Desktop/tablet; populated/empty/filtered | Dedicated matrix and browser comparison pass. | Passing | Separate Chrono and Scene Studios supersede the original single entry; relationship hierarchy remains binding. |
| 013 — Scenario Passport | Compact Crown-attached Passport owns provenance and package actions; structural commands stay in approved containment. | `ScenarioPassportPopover.jsx`; Crown identity projection; App/renderer package commands | `scenarioPassportContainment.test.js`; Scenario draft transaction tests; focused live E2E | Open Passport from Crown → inspect provenance → download package → dirty direct field → verify reason-disabled package actions → close/reopen/discard → switch to View | 1200×900 E2E; 1280×720 inspected; clean/dirty/closed/View | `0d23616` moves direct Scenario identity edits, source provenance, and exact package actions into the Crown-attached Passport; removes the generic Build auxiliary/package controls; preserves a closed dirty draft with an Unsaved marker; and keeps View authoring-free. Focused semantic/composition tests pass 31/31; the exact live journey including a JSON download passes 1/1. In-app inspection measured a 430×550 Passport fully inside a 1280×720 viewport with no root overflow; View reported zero Passport, trigger, or package controls. | Passing | None. |
| 014 — View exploration | Focus/comparison remain available in authoring-free View; Chrono date overlay avoids collisions and suspends/restores around focus/comparison. | `DashboardModeWorkspace.jsx`; `PlaybackSurface.jsx`; `ChronoDateOverlay.jsx`; `ChartPanel.jsx`; focus/comparison display state | Live state/composition tests and exact View E2E | Open Chrono → pointer/keyboard move and resize → enter Focus/Comparison → verify pause/suspension → exit and restore geometry/frame/cadence | 1200×900, 768×1024, 390×844 View | `9b4dd79` wires suspension through the production workspace, keeps the controller/overlay mounted but hidden so local geometry survives, pauses playback, restores the exact frame/cadence/overlay rectangle after Focus and Comparison, and prevents Focus hold suppression from swallowing comparison selections. The exact journey also verifies the dedicated resize corner, Arrow and Shift/Arrow paths, pointer movement/resize, 44px phone controls, tablet/phone reflow, and zero document overflow; it passes 1/1 and was visually inspected in-app. | Passing | None. |
| 015 — Context Shelf | Formal coordinated parking/resume preserves independent layout/chart slots plus one auxiliary draft, with named state and deterministic focus/scroll/target restoration. | Live draft coordinator; `BuildWorkspace.jsx` Context Shelf | Live integration plus coordinator matrix tests | Dirty both primary slots → open incompatible auxiliary → park/resume without mutation | Desktop/tablet; clean/dirty/error/suspended | `68b9c4d` wires the formal coordinator and named slots; `fd21b06` removes the legacy lock, explicitly suspends Unit Orbit after capturing restoration, and restores it on auxiliary close. Coordinator/live/restoration tests pass 24/24 and the exact dirty-layout + dirty-chart + Structure journey passes 1/1 in production E2E. | Passing | None. |
| 016 — Collection controls | Collection timing/paging controls live in the chart header, coexist with rightmost Build Edit, and avoid chart-state collisions. | `CollectionCarousel.jsx`; `CollectionPager.jsx`; `CollectionDisplay.jsx`; `CardChartView.jsx`; `TargetCollectionChartView.jsx`; `ChartPanel.jsx` | `collectionHeaderControls.test.js`; focused Collection/component suite; exact live E2E | Page/pause/resume a Collection in View; enter Build; retain the same 44px header and rightmost Edit action | 1280×720 View/Build; accepted multi-page 2×1 fixture; active/passive runtime | `76bb837` embeds dots and Previous/Play-Pause/Next into the canonical chart header and leaves Build Edit rightmost. Focused semantic/composition checks pass 60/60; the exact View/Build journey passes 1/1. In-app inspection measured a 581.5×418 panel with a 555.5×44 header, 265.5px readable title region, 280px transport, all three controls, and zero root overflow. | Passing | The sketch's “wide” physical 2×1 footprint maps to the production `standard` size token; the physical invariant is preserved. |
| 017 — chart data states | Normative Partial remains distinct, identifies unavailable series, preserves the last valid plot, and offers Continue with Available Data; all other accepted states retain ownership/recovery. | `ChartDataStateBoundary.jsx`; `ChartStateSurface.jsx`; shared `ChartView` consumers | `chartPartialState.test.js`; `chartStateSurface.test.js`; `chartDataState.test.js` | Render Partial → inspect available/unavailable series → Continue → verify state and saved semantics remain | Desktop View/Build harness; shared ordinary View and Comparison consumers; phone styling remains Step 9 UAT | RED proved live conversion to unavailable and missing action. GREEN: 13/13 focused tests; browser journey retained the Partial plate and available plot after Continue and announced unchanged saved semantics. | Passing | Step 7 verifies shared View/Build/Focus/Comparison semantics; Present/Audience controller placement remains Step 8. |
| 018 — source evidence | Ordinary chart evidence action opens the dedicated viewer directly; viewer owns provenance, complete rows, search/sort/pagination, exact load states, explicit return, and invoking-focus restoration. | `ChartPanelActions.jsx`; `SourceCsvViewerButton.jsx`; `sourceViewerProtocol.js`; dedicated `SourceCsvViewer.jsx` | `sourceEvidenceDirectJourney.test.js`; existing viewer/sort/theme suites; direct View and Build E2E journeys | Chart rail evidence action → dedicated viewer → inspect chart/variable/dataset/path and rows → Return to dashboard | 1280×720 View/Build; ready/popup-blocked; scrolled invoking chart | `28f0e1b` removes the Details/intermediary-modal dependency, adds protocol-v2 invocation provenance and a single-use return signal, and restores the exact invoking control without dashboard mutation. Focused semantic/composition checks pass 12/12; the dedicated View journey and updated Build restoration journey pass 2/2 against the production build. The browser run verified 177/177 rows, all four provenance identities, unchanged Page/scroll and saved layout, and returned focus. | Passing | The approved return guarantee upgrades the protocol-v1 limitation recorded by the sketch; the dedicated window remains the selected containment. |
| 019 — recovery rails | STATE-00 occupies only the application frame with no mode/Page workspace; every valid descendant scope keeps its surviving context and places exact recovery copy/action in the missing object's canonical region. | `ApplicationRecovery.jsx`; App recovery controller; `StructureAuthoring.jsx`; View empty-state renderer; `PresentWorkspace.jsx` | Application/structure/View/Present integration suites; exact zero-Page/zero-Section E2E | Invalid Scenario → Reload/Import; remove last Section/Page → repair inline; zero catalogue → route to Build | Desktop/tablet; application failure, zero Page/Section/panel/group/catalogue, storage errors | `39888f4` makes zero Page and zero Section drafts reachable, preserves valid ancestor context, supplies exact inline replacements, and rejects invalid Save. Current recovery integration passes 21/21; the exact zero-Page/zero-Section browser ladder passes 1/1 and was inspected at 1200×900. STATE-00 already matches the accepted no-mode-workspace application-frame composition. | Passing | The later gap report's claim that STATE-00 must retain the dashboard shell is rejected as contradictory: the approved README explicitly requires mode/Page chrome to disappear and no mode-owned workspace to render. |
| 020 — Page/Section commands | Anchored Page Orbit and Section command surfaces support destination, placement, merge/move/remove, disposition, and named structural/temporal consequences. | `BuildPageNavigation.jsx`; `SectionStructureCommandDialog.jsx`; `buildLayoutDraft.js`; canonical Build canvas | Live component/model tests plus `v3-anchored-structure-commands.spec.js` | Select active Page → open Orbit; move Section → choose destination/placement → inspect named proof → preview → discard | 1280×720; valid/protected/dirty; cross-Page; Page/Section removal dispositions | `8e1b65b` provides the ordered Edit/Earlier/Later rail, exact Rename/Merge/Remove Page Orbit, pinned Add Page, direct Section rename, complete local Section commands, eligible destinations/placements/dispositions, reason-disabled protected states, and accurate named dependency proof. Focused checks pass 11/11; live journey passes 1/1 and browser inspection confirms target clearance and no overflow. | Passing | None. |

## Completion gate

- Active Step 7 rows Passing: **16/16** (002, 003, 004, 005, 006, 007, 011, 012, 013, 014, 015, 016, 017, 018, 019, 020).
- Active Step 7 rows Missing or Partial: **0/16**.
- Deferred Step 8 rows: **001, 008**.
- Prior-step/shared-shell rows retained for truthful cross-step status: **009, 010**.
- Step 7 master-review readiness: **Yes — ready to submit for master review; master acceptance is not claimed.**

The matrix is updated in the same slice as each approved repair and its deterministic/browser evidence. Final submission must separately state engine implemented, UI implemented, and fidelity verified.
